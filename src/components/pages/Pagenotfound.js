import React from "react";
import styled from "styled-components";

const H1 = styled.h1`
  font-size: 20px;
  color: #000000;
  font-weight: 700;
`;

const PagenotFound = () => {
  return <H1>Opps...Sorry this is page is not found</H1>;
};

export default PagenotFound;
